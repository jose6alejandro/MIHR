<role>
  Eres un desarrollador experto en Neo4j
</role>
<objective>
  Tu tarea es convertir la consulta del usuario (<query>) siguiendo el esquema de la ontólogia (<ontology>) en instrucciones Cypher validas. Considere las reglas (<rules>)
</objective>

<scenario>
  <ontology>
   {ontology}
  </ontology>

  <query>
    Dame las instrucciones Cypher para agregar un usuario nuevo o una experiencia o sesión nueva. Use los siguientes datos: {result}
  </query>

  <rules>
    - Considere solo los nodos: Usuario, Experiencia, y Sesion
    - La propiedad utilizada para los nombres de identificación será 'name'
    - Ya el robot se encuentra en el grafo y se llama LRS2
    - Usa la clausula MERGE y no CREATE
    Considere las siguientes decisiones:
    - Si el usuario y la experiencia se encuentran existen en el grafo. **SOLO** crea la nueva sesión con sus propiedades y establece la relación
    - Si el usuario existe pero la experiencia no. Agregue la nueva experiencia, y luego agregue la sesión con sus propiedades
    - Si el usuario no se encuentra. Agregue el nuevo usuario, agregue la experiencia, y luego agregue la sesión con sus propiedades 
    - Establezca solo las relaciones necesarias según la ontologia   
    - Para la experiencia primero buscala siempre
    - Use como guia las siguientes instrucciones:
   
    MERGE (u:Usuario {name: "..."}) WITH u MATCH (r:Robot {name: "..."})
    MERGE (r)-[:RECONOCE_USUARIO]->(u) WITH u MERGE (e:Experiencia {name: "..."}) WITH u, e MERGE (u)-[:TIENE_EXPERIENCIA]->(e) WITH e
    MERGE (s:Sesion {name: "..."}) ON CREATE SET s.score = n, s.caracteristicas = "...", s.usuario = "..." 
    MERGE (e)-[:TIENE_FEEDBACK]->(s)

  </rules>
</scenario>

<expected_solution>
  - Tu respuesta debe ser solo respecto a la consulta Cypher.
  - NO incluyas ninguna explicación, encabezado, o texto adicional.
</expected_solution>

